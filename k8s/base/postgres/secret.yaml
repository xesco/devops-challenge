# Placeholder — real values injected via kubectl:
#
#   kubectl -n moonpay create secret generic postgres-credentials \
#     --from-literal=POSTGRES_USER=<user> \
#     --from-literal=POSTGRES_PASSWORD=<password> \
#     --from-literal=POSTGRES_DB=currencies
#
# This file exists so Kustomize can reference the resource. The empty `data`
# block is overwritten by the kubectl command above.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: moonpay
type: Opaque
data: {}
